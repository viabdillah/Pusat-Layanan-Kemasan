# Dependencies
/node_modules
/client/node_modules
/server/node_modules

# Build output
/client/dist          # Output build untuk proyek client (Vite/React)
/server/dist          # Output build untuk proyek server (jika ada)

# Configuration Files (Environment Variables)
.env
/client/.env
/server/.env

# Lock files (Opsional, tergantung preferensi)
# Biasanya package-lock.json dilacak, tetapi Anda mungkin ingin mengabaikannya jika memiliki banyak sub-package.
# Jika Anda menggunakan Yarn atau pnpm:
yarn.lock
pnpm-lock.yaml
/client/package-lock.json
/server/package-lock.json

# Local and Temporary Files
.DS_Store
*.log
npm-debug.log*

# IDE/Editor Files (VS Code)
.vscode/

# Client Specific Ignored Files (Vite/React)
/client/.vite
/client/temp

# Server Specific Ignored Files (Database/Uploads)
# Hapus baris ini jika Anda ingin meng-commit folder uploads
/server/uploads
/server/logs